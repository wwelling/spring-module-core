logging:
  path: logs
  file: ${logging.file.path}/spring-domain-tests.log
  level:
    org:
      folio: WARN
      hibernate: WARN
      springframework: WARN
      springframework.test: INFO

      # Uncomment to enable MockMvc unit test logging.
      #springframework.test.web.servlet.result: DEBUG
